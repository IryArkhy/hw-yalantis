import React from 'react';
import Layout from '../../components/Layout';
import styles from './user-profile.module.css';

const UserProfile = () => {
  return (
    <Layout>
      <h2 className={styles.cartPageTitle}>User profile page</h2>
    </Layout>
  );
};

export default UserProfile;
